<% 
// Variables pour le layout
const title = "Mot de passe oublié - MoneyWise";
%>

<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8">
        <!-- Header -->
        <div class="text-center">
            <div class="mx-auto h-24 w-24 bg-gradient-to-br from-orange-500 to-red-500 rounded-full flex items-center justify-center shadow-lg mb-6">
                <i class="fas fa-key text-3xl text-white"></i>
            </div>
            <h2 class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-2">
                Mot de passe oublié ?
            </h2>
            <p class="text-gray-600 dark:text-gray-400 max-w-sm mx-auto">
                Pas de souci ! Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
            </p>
        </div>

        <!-- Success/Error Messages -->
        <% if (typeof message !== 'undefined' && message) { %>
            <div class="bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 rounded-lg p-4">
                <div class="flex items-center">
                    <i class="fas fa-check-circle text-green-500 mr-3"></i>
                    <div>
                        <h3 class="text-sm font-medium text-green-800 dark:text-green-200">Email envoyé !</h3>
                        <p class="text-sm text-green-700 dark:text-green-300 mt-1"><%= message %></p>
                    </div>
                </div>
            </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg p-4">
                <div class="flex items-center">
                    <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
                    <div>
                        <h3 class="text-sm font-medium text-red-800 dark:text-red-200">Erreur</h3>
                        <p class="text-sm text-red-700 dark:text-red-300 mt-1"><%= error %></p>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Form Card -->
        <div class="bg-white dark:bg-gray-800 shadow-xl rounded-xl px-8 py-10 border border-gray-200 dark:border-gray-700">
            <form class="space-y-6" action="/auth/forgot-password" method="POST">
                <!-- Email -->
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                        <i class="fas fa-envelope mr-2 text-primary-600 dark:text-primary-400"></i>
                        Adresse email
                    </label>
                    <input id="email" name="email" type="email" autocomplete="email" required 
                           class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 placeholder-gray-500 dark:placeholder-gray-400 transition-colors"
                           placeholder="votre@email.com"
                           value="<%= typeof email !== 'undefined' ? email : '' %>">
                </div>

                <!-- Info Box -->
                <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4">
                    <div class="flex items-start">
                        <i class="fas fa-info-circle text-blue-500 mr-3 mt-0.5"></i>
                        <div>
                            <h3 class="text-sm font-medium text-blue-800 dark:text-blue-200 mb-1">Comment ça marche ?</h3>
                            <ul class="text-xs text-blue-700 dark:text-blue-300 space-y-1">
                                <li>• Nous envoyons un lien sécurisé à votre email</li>
                                <li>• Le lien expire dans 1 heure</li>
                                <li>• Vérifiez aussi vos spams</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <div>
                    <button type="submit" id="submit-btn"
                            class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-lg text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500 dark:focus:ring-offset-gray-800 transition-all duration-200 transform hover:scale-[1.02]">
                        <span class="absolute left-0 inset-y-0 flex items-center pl-3">
                            <i class="fas fa-paper-plane group-hover:text-orange-300"></i>
                        </span>
                        Envoyer le lien de réinitialisation
                    </button>
                </div>
            </form>
        </div>

        <!-- Back to Login -->
        <div class="text-center">
            <a href="/auth/login" class="inline-flex items-center text-sm text-primary-600 hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300 transition-colors group">
                <i class="fas fa-arrow-left mr-2 group-hover:-translate-x-1 transition-transform"></i>
                Retour à la connexion
            </a>
        </div>

        <!-- Help Section -->
        <div class="bg-gray-50 dark:bg-gray-800 rounded-xl p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-sm font-semibold text-gray-900 dark:text-gray-100 mb-3 flex items-center">
                <i class="fas fa-question-circle text-primary-500 mr-2"></i>
                Besoin d'aide ?
            </h3>
            <div class="space-y-2 text-xs text-gray-600 dark:text-gray-300">
                <p>• Si vous ne recevez pas l'email, vérifiez votre dossier spam</p>
                <p>• Assurez-vous d'utiliser l'adresse email de votre compte</p>
                <p>• Le lien expire après 1 heure pour votre sécurité</p>
            </div>
            <div class="mt-4 pt-4 border-t border-gray-200 dark:border-gray-600">
                <p class="text-xs text-gray-500 dark:text-gray-400">
                    Toujours des problèmes ? 
                    <a href="/contact" class="text-primary-600 hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300 transition-colors">
                        Contactez notre support
                    </a>
                </p>
            </div>
        </div>

        <!-- Security Notice -->
        <div class="text-center">
            <div class="inline-flex items-center text-xs text-gray-500 dark:text-gray-400 bg-white dark:bg-gray-800 px-4 py-2 rounded-full border border-gray-200 dark:border-gray-700">
                <i class="fas fa-shield-alt text-green-500 mr-2"></i>
                Vos données sont protégées par un chiffrement SSL
            </div>
        </div>
    </div>
</div>

<script>
    // Form submission handling
    document.querySelector('form').addEventListener('submit', function(e) {
        const submitBtn = document.getElementById('submit-btn');
        const email = document.getElementById('email').value;
        
        if (!email) {
            e.preventDefault();
            return;
        }
        
        submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Envoi en cours...';
        submitBtn.disabled = true;
    });

    // Auto-focus email field if no message is displayed
    <% if (typeof message === 'undefined' || !message) { %>
        document.getElementById('email').focus();
    <%}%>
</script>